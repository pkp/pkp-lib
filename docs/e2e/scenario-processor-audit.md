# Scenario-endpoint parity ledger

Recreated with the harness rebuild (2026-07-27, PROGRESS restart step 2).
Contract: PRINCIPLES §"Architecture principles" 2 — a seeded scenario must
leave the same database state, fire the same hooks, and produce the same
notifications as a user performing the equivalent steps through the UI/REST
API. **Any change to a Processor requires a parity entry here before it
merges**; deliberate deviations are recorded as such, with rationale.

Format: one row per audited builder path per app. Evidence detail stays in
`.reports/`; this file records the verdict.

| Date | App | Builder path | Parity check | Verdict | Notes |
|---|---|---|---|---|---|
| 2026-07-27 | OJS | submission · submit | Notifications + `event_log` + `stage_assignments` vs a submission created and submitted through the real REST API as `author.alex` | ✅ parity after fix | `Repo::submission()->submit()` alone omits `APPROVE_SUBMISSION`/`FORMAT_NEEDS_APPROVED_SUBMISSION` — the submit **controller** refreshes them via `NotificationManager::updateNotification()` after the repository call; the builder now calls that same service. |
| 2026-07-27 | OJS | submission · decisions | `edit_decisions`, `submissions.stage_id`, `review_rounds`, `event_log` after `sendExternalReview`/`accept`/`sendToProduction`/publish | ✅ real path | `Repo::decision()->validate()` + `->add()`; the app's own `ApplyDoneWorkflowStage` listener adds the unrequested `moveToDone` decision on publish, as in the UI. |
| 2026-07-27 | OJS | submission · reviewers | `review_assignments` (assigned/notified/confirmed/declined), `event_log` (`reviewerAssigned`, `reviewAccepted`) | ✅ real path | `EditorAction::addReviewer()` + the Add Reviewer form's post-assign `dateNotified`/`considered` stamp; accept/decline via `ReviewerAction::confirmReview()`; invitation body from the `REVIEW_REQUEST` template as the form posts it. |
| 2026-07-27 | OJS | context · scratch journal | Journal created through `app()->get('context')->add()` | ✅ real path | Genres, user groups, email templates, nav menus and the default section come from the real service; spec sections are matched to the auto-created default by abbrev and edited, so no stray "Articles" survives. |
| 2026-07-27 | shared | seeding mail | Mailpit count before/after a seed that invites + confirms a reviewer | ✅ suppressed | `Mail::fake()` for the seeding request only; `email_log` rows still written, so mail-log assertions keep working. |
| 2026-07-27 | OMP | submission · submit | `notifications`, `stage_assignments`, `event_log` vs a monograph created and submitted through the real REST path as `author.alex` into the same series | ✅ parity, no fix needed | Both sides carry `SUBMISSION_SUBMITTED` (×3, to the series editors), `APPROVE_SUBMISSION`, `FORMAT_NEEDS_APPROVED_SUBMISSION`; stage assignments identical. Stage A's OJS fix carries to OMP through the shared controller. Only diff: the control's extra wizard `PUT` adds a `metadataUpdated`. |
| 2026-07-27 | OMP | submission · review rounds (internal AND external) | `review_rounds.stage_id`/`round`/`status`, `review_assignments`, `edit_decisions` for internal-only, external-only, and both-in-one-spec seeds | ✅ real path | Rounds opened by real decisions chosen from where the submission stands — `sendInternalReview` (1→2), `skipInternalReview` (1→3), `sendExternalReview` (2→3) — never by writing a round. Internal round's status 4 (ACCEPTED) on `sendExternalReview` is OMP's own `getNewReviewRoundStatus()`: real behaviour reproduced, not mirrored. |
| 2026-07-27 | OMP | context · series | Series via `Repo::section()` (OMP's series repository; the DAO writes the `series` table) | ✅ real path | A press starts with NO series, so every seeded series is genuinely created; `image` initialised as `SeriesForm::execute()` does; duplicate paths throw rather than silently update. |
| 2026-07-27 | OMP | bootstrap/context · `users[].series` | `subeditor_submission_group` rows + end-to-end: the three `monographs` series editors appear as `stage_assignments` and `SUBMISSION_SUBMITTED` recipients on BOTH a seeded and a UI-path submission | ✅ real path | `SubEditorsDAO::insertEditor(contextId, seriesId, userId, ASSOC_TYPE_SERIES, userGroupId)` — the call OMP's `SeriesForm` delegates to — restricted to the form's `assignableRoles`. Unknown path / un-assignable role THROW with the `specKey`; idempotent re-runs skip. |
| 2026-07-27 | OMP | seeding mail | Messages to a throwaway OMP-specific recipient before/after seeding a scratch press + user | ✅ suppressed | 0 before, 1 after a real lost-password request; Mailpit never cleared, no global counts asserted (fleets share it). |
| 2026-07-27 | OPS | submission · submit | Notifications, `stage_assignments`, `event_log`, `email_log` vs a preprint created and submitted through the real submission controllers as `author.alex` | ✅ parity | Six notification types identical row-for-row and user-for-user, incl. the `APPROVE_SUBMISSION` pair from stage A's fix, carried unchanged. Stage assignments identical, incl. the two Moderators auto-assigned by the `SubmissionSubmitted` → `AssignEditors` listener. |
| 2026-07-27 | OPS | submission · publish (`published: true`) | `submissions.status/stage_id`, `edit_decisions`, reader-side reachability | ✅ real path | `Repo::publication()->validatePublish()` + `->publish()` as an editorial user (OPS's `canCurrentUserPublish()` requires it); `ApplyDoneWorkflowStage` records the unrequested `moveToDone` — reproduced, not simulated. Posted preprint anonymously reachable and listed; unposted one 404s. |
| 2026-07-27 | OPS | submission · `reviewRounds` refusal | Spec with a `reviewRounds` block | ✅ refused | `getReviewStages()` is empty on OPS, so the shared builder throws before touching anything: 400, `specKey reviewRounds`. |
| 2026-07-27 | OPS | bootstrap/context · `users[].sections` | `GET /publicknowledge/api/v1/submissions` in three real authenticated sessions after seeding a submitted preprint | ✅ real path | Assigned Moderator (`sectioneditor.ana`) sees the preprints; same-role unassigned `sectioneditor.omar` sees none (the control that makes the list mean something); manager sees all. Via `SubEditorsDAO::insertEditor(...)` restricted to `PKPSectionForm::$assignableRoles`. |
| 2026-07-27 | OPS | context · scratch server | Server via `app()->get('context')->add()` | ✅ real path | Genres/user groups/templates/menus/default section from the real service; base seed's `PRE` matches the auto-created default section and is EDITED, so exactly one survives. |
| 2026-07-27 | OPS | seeding mail | Mailpit scoped by recipient + OPS-only tag, positive control bounding the negatives | ✅ suppressed | 0 hits for seed tags, 1 for the real path's acknowledgement, same run; `email_log` still written both sides; never cleared, no global counts. |
| 2026-07-27 | OPS | ⚠ known scope gap · files | Real OPS wizard attaches a **galley** (`SUBMISSION_FILE_PROOF`); seeded preprints carry `SUBMISSION_FILE_SUBMISSION` and no galley | deliberate (step-2 scope) | Galleys are not step-2 schema in any app. Step-3 specs about files/galleys/reader download on OPS must seed their own galley until the schema grows one. |
| 2026-07-27 | OJS | bootstrap/context · `users[].sections` | `GET /publicknowledge/api/v1/users?assignedToSection={id}` (the Sections settings form's own query) for both seeded sections, each list the other's positive control | ✅ real path | `SubEditorsDAO::insertEditor(contextId, sectionId, userId, ASSOC_TYPE_SECTION, userGroupId)` — the call `PKPSectionForm::execute()` makes — restricted to that form's `assignableRoles` (sub-editor → manager → assistant preference; the PK allows one group per user per section). Unknown abbrev and un-assignable role both THROW with the offending `specKey`; idempotent re-runs skip existing rows. Read-back avoids `SubEditorsDAO::editorExists()` (broken pre-3.2 SQL — see stage-B report §7.3). |
