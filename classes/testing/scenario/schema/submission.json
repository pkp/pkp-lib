{
    "title": "Test-harness submission spec",
    "description": "The end state a test needs a submission to be in. The builder walks the application to that state through the real services; the test never scripts the journey.",
    "type": "object",
    "additionalProperties": false,
    "required": ["tag", "context", "submitter"],
    "properties": {
        "tag": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "description": "Parallel-isolation key. Appended to the submission title so assertions can attribute rows by tag rather than by id."
        },
        "context": {"type": "string", "minLength": 1, "description": "urlPath of the context to submit to."},
        "submitter": {"type": "string", "minLength": 1, "description": "username of the submitting user."},
        "title": {"type": "string", "minLength": 1},
        "abstract": {"type": "string"},
        "locale": {"type": "string"},
        "submitted": {
            "type": "boolean",
            "description": "Defaults to true. An EXPLICIT false seeds a wizard-resumable draft at parity: no dateSubmitted, submissionProgress set to the first wizard step, author canChangeMetadata."
        },
        "decisions": {
            "type": "array",
            "description": "Editorial decisions to record, in order, through the real decision service. Each drives the real stage progression, hooks and notifications.",
            "items": {
                "type": "object",
                "additionalProperties": false,
                "required": ["decision"],
                "properties": {
                    "decision": {
                        "type": "string",
                        "minLength": 1,
                        "description": "Decision type name, lower-camel-case of the decision type class (sendExternalReview, requestRevisions, accept, newExternalRound, sendToProduction, decline, ...). Validated against the app's own decision-type roster."
                    },
                    "editor": {"type": "string", "description": "username recording the decision; defaults to the first manager/editor enrolled in the context."}
                }
            }
        },
        "reviewRounds": {
            "type": "array",
            "description": "Reviewers per review round. Entry i targets review round i+1 of the submission's review stage; the round must exist (created by a promoting decision) or the app's default promoting decision is applied for round 1.",
            "items": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                    "reviewers": {
                        "type": "array",
                        "items": {
                            "type": "object",
                            "additionalProperties": false,
                            "required": ["user"],
                            "properties": {
                                "user": {"type": "string", "minLength": 1, "description": "username of the reviewer."},
                                "status": {
                                    "type": "string",
                                    "enum": ["invited", "accepted", "declined"],
                                    "description": "Defaults to invited. 'accepted'/'declined' additionally run the reviewer's own confirm action."
                                }
                            }
                        }
                    }
                }
            }
        },
        "published": {"type": "boolean", "description": "Publish the current publication through the real publish path."}
    }
}
