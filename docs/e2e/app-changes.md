# App changes & build blockers

Per `docs/product/RUNBOOK.md` "What goes where", this file records ONLY:

- **actual app-code changes** made by the campaign (what, why, where, commit);
- **build blockers**: app defects that prevented tests from running green
  (race conditions, nondeterministic UI, harness-hostile behavior) and the
  workaround taken.

Product findings — bugs, divergences, open questions — live in each feature
spec's Findings register, never here.

**Reset 2026-07-26**: the previous contents (8 production fixes and a 272-row
findings archive) were deleted and the fixes reverted (git history keeps both);
the restarted process rediscovers what matters on its own evidence.

One file for all three apps: name the app per row (`ojs` / `omp` / `ops`;
shared lib/pkp changes say `pkp-lib`).

| # | App | What & why | Files | Commit | Notes |
|---|-----|-----------|-------|--------|-------|
| 1 | pkp-lib | `CONFIG_FILE` honours a `PKP_CONFIG_FILE` env var (one line) so the e2e fleets run against `config.test.inc.php` without touching `config.inc.php`. Router-script/registry alternatives provably break: `PKPAppKey::refreshEncrypter()`/`writeAppKeyVariableToConfig()` reset to `CONFIG_FILE` mid-install, writing the app key into the dev config. No behaviour change when unset. | `classes/config/Config.php` | pending | Harness rebuild, stage A (2026-07-27) |
| 2 | ojs | `/config.test.inc.php` gitignored — holds local DB credentials + generated app key, like the already-ignored `/config.inc.php`. | `.gitignore` | pending | Harness rebuild, stage A (2026-07-27) |
| 3 | ojs + pkp-lib | New test-only files, inert without `TEST_API_KEY` in the process env (no app behaviour change): `api/v1/_test/*`, `tools/installTest.php`, `tools/testServer.sh` (ojs); `api/v1/_test/*`, `classes/testing/*` (pkp-lib). | see row | pending | Harness rebuild, stage A (2026-07-27) |
| 4 | pkp-lib | Test-API user specs gained a declared app overlay (`userSchemaOverlayProperties()` + `userSchema()`; `seedUsers()` calls a new `afterUserSeeded()` hook) so app concepts enter the shared user schema only by declaration — `sections` validates on OJS, rejected as unknown elsewhere. Test-only namespace. | `api/v1/_test/PKPTestApiController.php`, `api/v1/_test/BootstrapRoutes.php` | pending | Harness rebuild, stage B (2026-07-27) |
| 5 | ojs | `users[].sections` overlay — section-editor assignment via the real service (`SubEditorsDAO::insertEditor`, as `PKPSectionForm::execute()` calls it); makes the base roster's recorded section-editor assignments seedable. | `api/v1/_test/JournalScenarioController.php` | pending | Harness rebuild, stage B (2026-07-27) |
| 6 | ojs + pkp-lib | New test-only Playwright harness files (no app behaviour change): `playwright/*`, `.env.playwright.example`, nine `test:e2e:*` npm scripts, `@playwright/test` devDependency, `.gitignore` entries (ojs); `playwright/*` (pkp-lib). | see row | pending | Harness rebuild, stage B (2026-07-27) |
| 7 | ojs | `tools/installTest.php` self-heals from an empty or partially installed DB (pre-boot `installed`-flag management with a three-state schema probe), refuses to install over a populated DB (`--recreate-db` instead), exits non-zero on failure. The app cannot bootstrap against a DB with no `core` version row — the state a new contributor, CI, or an interrupted install starts from. Test-only tool. | `tools/installTest.php` | pending | Harness rebuild, stage A follow-up (2026-07-27) |
| 8 | omp | OMP fleet wiring, all test-only (no app behaviour change): `api/v1/_test/*` (PressScenarioController w/ series + `users[].series` overlays, submission overlay w/ per-round internal/external `stage` key, TestBootstrapController), `tools/installTest.php` + `tools/testServer.sh` (ported from OJS), `playwright/*`, `.env.playwright.example`, `test:e2e:*` scripts, `@playwright/test` devDependency, `.gitignore` entries (`config.test.inc.php`, run artefacts). | see row | pending | Harness rebuild, stage C (2026-07-27) |
| 9 | ops | OPS fleet wiring, all test-only (no app behaviour change): `api/v1/_test/*` (ServerScenarioController w/ sections + screening-settings overlays, submission overlay w/ `section`; `promoteToReviewDecision()` stays null — the absence IS the declaration), `tools/installTest.php` + `tools/testServer.sh`, `playwright/*`, `.env.playwright.example`, `test:e2e:*` scripts, `@playwright/test` devDependency, `.gitignore` entries. `users[].sections` is load-bearing on OPS: Moderators see only submissions they're assigned to, so without it every seeded preprint is invisible to every moderator. | see row | pending | Harness rebuild, stage C (2026-07-27) |
| 10 | all (pkp-lib) | Test-harness only: `user` test option accepts a `DEFAULT_EDITORIAL_USER` sentinel resolved by the `storageState` fixture through `appContext.seed.actors` (editor → manager fallback); the shared login smoke names no usernames and covers `asUser` on every app. The old hard-coded `editor.diana` broke OPS (no editor group) before capability skips could run. | `playwright/support/base-test.js`, `playwright/tests/login.spec.js` | pending | Harness rebuild, stage D (2026-07-27) |
| 11 | all (pkp-lib) | Test-harness only: `PKPSubmissionScenarioController::reviewStageId()` uses `max()` of the review-stage roster instead of `end()` (+ comment: the roster is unordered — OMP returns [EXTERNAL, INTERNAL]). Harmless today; removes a trap future shared code would inherit. | `api/v1/_test/PKPSubmissionScenarioController.php` | pending | Harness rebuild, stage D (2026-07-27) |
| 12 | all (pkp-lib) | Test-harness only: `invitations[]` scenario seed key (pending / already-expired, newcomer or existing account; response echoes `acceptUrl`/`declineUrl` + key, so a test drives the recipient's journey without scraping mail). Built because the ONLY way probing could reach an expired invitation was appending `[invitations] expiration_days = 0` to `config.test.inc.php` on a live shared fleet and restoring it seconds later — a manoeuvre that must never sit in a retained suite. Seeds via the wizard's own three controller calls; expiry alone is written directly (`Invitation::invite()` only stamps expiry forward and `setExpiryDate()` throws once dispatched), reading the window back from the app so the seed stays correct if the config changes. Recorded as a deliberate deviation in `scenario-processor-audit.md`. | `api/v1/_test/PKPContextScenarioController.php`, `classes/testing/scenario/schema/context.json`, `classes/testing/scenario/BuildJournal.php` | pending | U6 test authoring (2026-07-28) |
| 13 | ojs (defect in pkp-lib UI) | Build blocker, worked around in the TEST layer — no app code changed: the invite wizard's role table gives every row the same input ids (`id="-userGroupId-control"` …), so added rows' role/date/masthead controls have no accessible name and the first row's name grows a copy of the label per row. `getByRole`/`getByLabel` cannot address rows, so `InviteUserWizardPage` locates rows structurally and their fields by form `name` (`select[name="userGroupId"]`, `input[name="dateStart"]`, `select[name="masthead"]`). The underlying defect is a product finding and is filed in the U6 spec's Findings register, not here. | `playwright/pages/InviteUserWizardPage.js` (workaround); defect in `lib/ui-library/src/pages/userInvitation/UserInvitationUserGroupsTable.vue` | pending | U6 test authoring (2026-07-28) |
| 14 | omp (defect in pkp-lib UI) | Build blocker, worked around in the TEST layer — no app code changed: the Users & Roles row-options button's accessible name is the untranslated key `##userAccess.management.options##`, so a `getByRole('button', {name})` locator would have to hard-code the defect string and would break the moment it is fixed. `UsersAndRolesPage.openUserMenu()` addresses the control structurally (the row's last button) instead. The underlying defect is a product finding and is filed in the U53 spec's Findings register, not here. | `playwright/pages/UsersAndRolesPage.js` (workaround); defect in `lib/ui-library/src/managers/UserAccessManager/UserAccessManagerCellActions.vue` | pending | U53 test authoring (2026-07-29) |
| 15 | all (pkp-lib) | Test-harness only: `users[].roles` accepts `siteAdmin` (context-scenario and bootstrap endpoints), enrolled through the installer's own `Repo::userGroup()->assignUserToGroup()`; rejected in `invitations[].roles`. Built because **no screen in any of the three apps grants the site-administrator role** (`UserForm::saveUserGroupAssignments()` intersects posted group ids with the context's own groups and the admin group is in no context), so the installer's read-only `admin` was the only administrator an install had — and every administrator-behaviour question was undetermined for want of a disposable one. Recorded as a deviation-by-absence in `scenario-processor-audit.md`: the parity target does not exist, so live end-to-end verification is the substitute. | `api/v1/_test/PKPTestApiController.php`, `classes/testing/scenario/schema/user.json` | pending | U53 probing (2026-07-29) |
